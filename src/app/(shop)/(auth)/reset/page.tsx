import ResetPasswordClient from './client'

export const metadata = {
  title: 'Reset Password | Nutflick',
}

const ResetPasswordPage = () => <ResetPasswordClient />

export default ResetPasswordPage
